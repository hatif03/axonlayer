# Mainnet Facilitator Environment Variables
# Copy this file to .env and fill in your actual values

# REQUIRED: At least one private key must be provided
# EVM Private Key (for Ethereum, Polygon, Base, etc.)
# Format: 64-character hex string without 0x prefix
EVM_PRIVATE_KEY=your_ethereum_private_key_here

# SVM Private Key (for Solana)
# Format: Base58 encoded private key
SVM_PRIVATE_KEY=your_solana_private_key_here

# OPTIONAL: Server Configuration
PORT=3000
NODE_ENV=development

# Note: The facilitator currently supports:
# - EVM Networks: polygon-amoy (testnet), polygon (mainnet)
# - SVM Networks: solana-devnet (currently commented out in code)
